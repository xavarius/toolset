# clone
git clone https://github.com/powerline/fonts.git --depth=1
# install
cd fonts
./install.sh
# clean-up a bit
cd ..
rm -rf fonts


echo "Please remember to setup powerline (f.ex maslo powerline) based font in iterm -> preferences -> ..."
